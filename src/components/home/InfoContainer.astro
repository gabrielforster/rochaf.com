---
import type { Experiences } from "../../types";

const { title, details } = Astro.props as Experiences;
---

<section
  class="flex items-start justify-between flex-col sm:flex-row dark:text-light"
>
  <h2 class="w-[15rem] mt-16 text-xl underline">{title}</h2>
  <div class="w-full">
    {
      details.map((item) => (
        <div class="my-16" id={item.id}>
          <div class="flex justify-between items-center flex-wrap">
            <h3 class="text-xl font-semibold">{item.name}</h3>
            <p class="text-sm dark:text-gray">
              {item.startDate && new Date(item.startDate).toLocaleDateString()}
              {item.endDate && " - " + new Date(item.endDate).toLocaleDateString()}
            </p>
          </div>
          <p class="text-sm text-blue">{item.location}</p>
          <p class="text-md mt-3">{item.description}</p>
          <ul class="mt-3">
            {item.links.map((item) => (
              <li class="text-sm mt-[1px] underline">
                <a href={item.url} target="_blank">
                  {item.label}
                </a>
              </li>
            ))}
          </ul>
          <div class="w-full mt-2">
            <p>Stack</p>
            <ul class="flex">
              {item.tags.map((item) => (
                <li class="dark:text-gray pr-2">{item}</li>
              ))}
            </ul>
          </div>
        </div>
      ))
    }
  </div>
</section>
